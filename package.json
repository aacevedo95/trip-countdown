{
  "name": "trip",
  "version": "0.0.1",
  "homepage": "https://ziekhart.github.io/trip-countdown",
  "devDependencies": {
    "react-scripts": "0.8.4"
  },
  "dependencies": {
    "gh-pages": "^2.0.1",
    "react": "^16.4.0",
    "react-dom": "^16.4.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "deploy": "gh-pages -d build",
    "predeploy": "npm run build",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  }
}
